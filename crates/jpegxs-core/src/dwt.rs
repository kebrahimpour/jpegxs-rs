use anyhow::Result;

pub fn dwt_53_forward_2d(
    _input: &[f32],
    _output: &mut [f32],
    _width: u32,
    _height: u32,
) -> Result<()> {
    todo!("5/3 DWT forward transform")
}

pub fn dwt_53_inverse_2d(
    _input: &[f32],
    _output: &mut [f32],
    _width: u32,
    _height: u32,
) -> Result<()> {
    todo!("5/3 DWT inverse transform")
}

pub fn dwt_97_forward_2d(
    _input: &[f32],
    _output: &mut [f32],
    _width: u32,
    _height: u32,
) -> Result<()> {
    todo!("9/7 DWT forward transform")
}

pub fn dwt_97_inverse_2d(
    _input: &[f32],
    _output: &mut [f32],
    _width: u32,
    _height: u32,
) -> Result<()> {
    todo!("9/7 DWT inverse transform")
}
