use crate::types::Bitstream;
use anyhow::Result;

pub fn pack_bitstream(_data: Vec<Vec<u8>>) -> Result<Bitstream> {
    todo!("Pack bitstream")
}

pub fn unpack_bitstream(_bitstream: &Bitstream) -> Result<Vec<Vec<u8>>> {
    todo!("Unpack bitstream")
}
